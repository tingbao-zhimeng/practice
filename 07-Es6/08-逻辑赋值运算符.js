// ??= &&= ||=
// 或赋值运算符x||= y// 等同于x ||(x = y)
// 与赋值运算符x 88= y// 等同于x &&(x= y)
// Nu11 赋值运算符x ??= y// 等同于x ??(x = y)


{
  一真全真//或赋值运算符
  // let id = 100
  // id =id || 99;
  //console.log(id);
  // id||(id=99);
  // console.log(id);
}

{
  // id ||= 99; 
  // console.log(id)
  console.l0g("******")
  //与赋值运算符 && 一假全假
  // let id = 10;
  // id = id && 99;
  // console.log(id);

}